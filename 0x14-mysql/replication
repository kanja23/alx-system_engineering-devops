# CREATE DATABASE tyrell_corp;

# USE tyrell_corp;
# CREATE TABLE nexus6 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50)
);
# INSERT INTO nexus6 (name) VALUES ('Replicant 1');
# GRANT SELECT ON tyrell_corp.nexus6 TO 'holberton_user'@'localhost';
# FLUSH PRIVILEGES;
